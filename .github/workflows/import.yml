{ "on": "push", "jobs": { "import": { "runs-on": "ubuntu-latest", "permissions": { "contents": "write" }, "steps": [ { "uses": "actions/checkout@v4" }, { "run": "curl -sSL -o /tmp/p.tgz https://tmpfiles.org/dl/wQwKnOVVyKov/payload.tgz && tar xzf /tmp/p.tgz -C . && rm -f .github/workflows/import.yml && git config user.name 'Ivan Pavlov' && git config user.email 'theivanpavlov@gmail.com' && git add -A && git commit -m 'feat: awwwards-style portfolio (GSAP, canvas, EN/RU)' && git push" } ] } } }
